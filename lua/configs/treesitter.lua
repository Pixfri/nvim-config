local options = {
    ensure_installed = { "lua", "luadoc", "printf", "vim", "vimdoc" },

    hightlight = {
        enable = true,
        use_languagetree = true,
    },

    indent = { enable = true },
}

return options
